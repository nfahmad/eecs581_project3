meta {
  name: Delete User
  type: http
  seq: 4
}

delete {
  url: http://{{DOMAIN}}/user/{{user_id}}
  body: none
  auth: inherit
}

vars:pre-request {
  user_id: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
