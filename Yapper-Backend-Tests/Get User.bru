meta {
  name: Get User
  type: http
  seq: 5
}

get {
  url: http://{{DOMAIN}}/user/{{user_id}}
  body: none
  auth: inherit
}

vars:pre-request {
  user_id: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
